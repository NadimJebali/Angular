import { CostumDirDirective } from './custom-dir.directive';

describe('CustomDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CostumDirDirective();
    expect(directive).toBeTruthy();
  });
});
